# Web Push VAPID configuration. Keep the private key in .dev.vars locally
# and store all three values with `wrangler secret put` in production.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
